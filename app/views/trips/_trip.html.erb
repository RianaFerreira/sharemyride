<tr>
  <td><%= link_to trip.dept_date.strftime("%a, %m-%d-%y"), trip_path(trip.id) %></td>
  <td><%= trip.start_point.name %></td>
  <td><%= trip.end_point.name %></td>
  <td><%= trip.num_seats %></td>
  <td><%= number_to_currency(trip.seat_cost) %></td>
</tr>